apiVersion: v1
kind: Service
metadata:
  name: cadvisor
  namespace: kube-system
  labels:
    app: cadvisor
spec:
  type: ClusterIP
  selector:
    app: cadvisor
  ports:
    - port: 8080
      targetPort: 8080
